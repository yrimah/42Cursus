input {
    file {
        path => "/logstash_dir/inlog.log"
        codec => "line"
    }
}

output {
    stdout { codec => rubydebug }
    elasticsearch {
        hosts => ["http://elasticsearch:9200"]
        index => "myindex"
    }
}